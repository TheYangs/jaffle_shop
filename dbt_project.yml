name: 'jaffle_shop'

config-version: 2
version: '0.1'

profile: 'jaffle_shop'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

models:
  jaffle_shop:
      materialized: table
      staging:
        materialized: view

dispatch:
  - macro_namespace: dbt_utils
    search_order: [dbt_utils]


# on-run-start: "{{ get_source_relation_lists2('dbt_alice') }}"
# on-run-start: "{{ get_source_relation_lists('dbt_alice') }}"
# on-run-start: "{{ log(get_source_tables('dbt_alice')) }}"
# on-run-start: "{{ log(test_get_source_tables('dbt_alice')) }}"
# on-run-start: "{{ codegen.generate_source('dbt_alice') }}"
